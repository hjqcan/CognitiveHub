# Only needed for the opt-in live Jev example. Never commit a real key.
TYPESAFE_AI_API_KEY=
JEV_MODEL=jev-1.13.0

# Optional: run the store conformance suite against a real PostgreSQL server with `npm run test:pg`.
# Install the driver first (`npm install --no-save pg`) and point at a throwaway database: migrate() creates cognitive_hub_* tables.
COGNITIVE_HUB_PG_URL=
