# 后续路线与验收

## 已完成：foundation

可信模块安装、依赖解析、作用域能力、租约排空；模型独立的认知单步；Jev HTTP 合同；默认预览、状态与授权二次检查；内存执行日志与未知结果核对；两个无外部副作用的宿主示例。

## v0.2：可恢复的意图运行时

目标：宿主交付一个范围明确的目标后，Hub 能持续推进，需要慢思考时停下来，得到回应后继续，进程重启后也知道之前做到哪里。分四步，全部用合成样本验收，真实宿主接入放在 v0.2 之后。

| 步骤 | 交付 | 状态 |
| --- | --- | --- |
| PR 1 | 稳定动作身份（不含 activation）、journal 导出/导入、未终态记录按精确插件版本重新绑定后查询核对、机器可读拒绝码、重启示例 | 已完成 |
| PR 2 | Run 契约与状态机、`step(runId)` 串行推进、GoalEvaluator、结构化等待条件、四类慢思考回应、预算与无进展检测、Run 所有权租约、通用模拟宿主与真实 SIGKILL 验收 | 已完成 |
| PR 3 | PostgreSQL 存储：`./pg` 子路径、注入式 SQL 客户端、每个操作一条语句、与内存实现共用一套一致性测试、验收链路在 PGlite 上重跑 | 已完成 |
| PR 4 | DecisionRecord（只来自 propose 已计算的内容）、内存与 PostgreSQL 决策存储、`timeline()` / `reevaluate()`、只读回放 CLI | 已完成 |

v0.2 四步全部完成。PR 3 的 SQL 在 PGlite（编译成 WebAssembly 的 PostgreSQL）上离线验证；设置 `COGNITIVE_HUB_PG_URL` 并安装 `pg` 可对真实服务器运行同一套测试，CI 未接入真实服务器。SIGKILL 链路仍在文件存储上验证，PostgreSQL 上只做进程内重启。

## v0.3：消费者加固与旁路模式

三个游戏（飞船、弹幕、浇给）接入 v0.2 后暴露了同一批问题，v0.3 把它们收进内核，并为阶段 A 准备旁路模式。全部向后兼容，每步都以三个游戏的回归检查为门槛（见 [v0.3 回归报告](game-integration-v0.3.md)）。

| 步骤 | 交付 | 状态 |
| --- | --- | --- |
| M1 | 单步开销与历史无关：终态游标、到期查询走索引、安静检查零写入、观测复用、只拷贝补丁（ADR 0007） | 已完成 |
| M2 | 单步可追溯：`decisionId`、`Decision.provider`、轮次阶段、结构化请示原因、`StepResult` 的记录与失败码 | 已完成 |
| M3 | 决策器失败策略 `onDecisionError: 'wait'`，按无进展计数（ADR 0008） | 已完成 |
| M4 | 自由文本 4096 字符上限、统一意图校验、Jev 内置选项可配置 | 已完成 |
| M5 | 多 Run 宿主：`stepDue()`、`limitDecider()`、`stopAll()`（ADR 0009） | 已完成 |
| M6 | 保留策略：三种 `prune`、`runtime.prune()`、`recordFacts`、schema v4 | 已完成 |
| M7 | 旁路模式 `approval: 'advisory'` 与 `compareAdvice()`（ADR 0010） | 已完成 |
| M8 | 0.3.0 发布：文档同步、CI 接入真实 PostgreSQL | 已完成 |

v0.4 候选，按价值排序：
1. 阶段 A 的真实宿主只读接入。内核侧已就绪，需要平台访问与标注数据。
2. Run 内部列表的压缩或外移，让单步写入的字节也与历史无关。
3. SIGKILL 链路在 PostgreSQL 上验证。
4. 取消/补偿端口，等真实执行器提出需求。
5. C# 接入端与 MCP 桥。

以下阶段保持原有描述。

## 阶段 A：一个真实宿主的只读/旁路接入（v0.2 之后）

接入 RMS/RBS 现有状态，不下发真实运动。将实际异常整理成 Observation，人工评估候选召回、决策建议和拒绝行为。v0.3 已提供内核侧支持：`approval: 'advisory'` 的旁路 Run 只提案与预检、从不派发，`compareAdvice()` 与 `scripts/replay.mjs --labels` 按平台实际决定给出一致率与召回率（步骤见 `docs/integration.md`）。剩下的是真实宿主的只读状态适配器与标注数据，需要平台访问。

验收：不改认知内核即可更换适配器；Hub 关闭时原平台照常工作；敏感数据和权限不进入模型；不能把“旁路建议合理”当作“执行一定成功”。

## 阶段 B：可靠执行基础

已完成：单进程重启后的记录核对、插件版本固定与在途记录重新绑定、PostgreSQL journal 与 run store、Run 所有权租约、托管人工请求的内嵌 outbox 与至少一次投递、记录保留与清理。剩余：分布式调度与多写者、自动恢复循环、鉴权与审计、SIGKILL 链路在 PostgreSQL 上的验证。

验收：在 claim、发送、回执和验证各阶段故障注入；重启后不会重复产生副作用；未知请求可按稳定幂等键核对；不宣称没有执行端去重就能 exactly-once。

## 阶段 C：受限真实操作

选择低风险、可检查、可停止的正式任务。加入权威执行网关、条件写入、权限变更、维护窗口及操作预算。物理风险由独立控制与安全机制约束。

验收：不越权、不争夺原平台控制权、不把接受当完成；减少无效处理和人工负担；完整覆盖取消、恢复、并发与回滚限制。

## 阶段 D：插件生态与跨宿主 SDK

协议版本管理、.NET SDK、远程插件 worker、签名/来源治理、声明式 profile、MCP 桥、记录/回放与观察面板。

验收：数字产品、人形机器人、平台管理层主要通过不同插件组合接入，而不是各复制一份 Hub。第三方代码隔离需要真正的进程/部署权限边界。

暂不排期：任意自然语言到正确计划、模型自主安装插件、机器人端到端控制、在线自动修改授权规则。
